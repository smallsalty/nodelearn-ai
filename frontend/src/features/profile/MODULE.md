# 前端画像边界

- 阅读 `docs/modules/profile.md`。
- 使用 `src/types/profile.ts`。
- 使用 `src/api/modules/profile.ts`。
- 页面壳：`src/pages/ProfilePage.vue`。
- Vue 组件不得直接请求后端。
- 课程、知识点和枚举值必须经过现有 API 数据与中文格式化函数转换后展示，不直接显示内部编号或英文枚举。
